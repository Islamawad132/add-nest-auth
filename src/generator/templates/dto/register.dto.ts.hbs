import { IsEmail, IsNotEmpty, IsString, MinLength, MaxLength{{#if rbac.enabled}}, IsArray, IsOptional{{/if}} } from 'class-validator';

export class RegisterDto {
  @IsEmail()
  @IsNotEmpty()
  email: string;

  @IsString()
  @IsNotEmpty()
  @MinLength(8)
  @MaxLength(32)
  password: string;
{{#if rbac.enabled}}

  @IsArray()
  @IsOptional()
  roles?: string[];
{{/if}}
}
